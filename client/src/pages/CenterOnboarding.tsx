import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { ArrowRight, Building2, ChevronLeft, KeyRound, MapPin, Phone, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";

type StartMode = "choose" | "create" | "created";

export default function CenterOnboarding() {
  const { isAuthenticated, loading } = useAuth();
  const [, navigate] = useLocation();
  const [mode, setMode] = useState<StartMode>("choose");
  const [centerName, setCenterName] = useState("");
  const [organizationName, setOrganizationName] = useState("");
  const [city, setCity] = useState("");
  const [phone, setPhone] = useState("");
  const [description, setDescription] = useState("");
  const createCenter = trpc.onboarding.createIndependentCenter.useMutation({
    onSuccess: () => { toast.success("تم إنشاء مركزك المستقل وربط حسابك كمدير للمركز"); setMode("created"); },
    onError: (error) => toast.error(error.message || "تعذر إنشاء المركز"),
  });

  if (!loading && !isAuthenticated) return <main className="access-redemption-shell" dir="rtl"><section className="glass-card rounded-3xl p-6 text-center sm:p-8"><KeyRound className="mx-auto h-10 w-10 text-primary" /><h1 className="mt-4 font-display text-2xl font-bold">ابدأ بتسجيل الدخول</h1><p className="mt-2 text-sm leading-7 text-muted-foreground">سجّل الدخول أولاً ثم اختر إنشاء مركز جديد أو الانضمام لمركز قائم.</p><button onClick={() => navigate("/login")} className="btn-gold mt-6 w-full py-3">تسجيل الدخول</button></section></main>;

  const submit = (event: React.FormEvent) => {
    event.preventDefault();
    if (!centerName.trim()) { toast.error("اكتب اسم المركز"); return; }
    createCenter.mutate({ centerName: centerName.trim(), organizationName: organizationName.trim() || undefined, city: city.trim() || undefined, phone: phone.trim() || undefined, description: description.trim() || undefined });
  };

  return <main className="access-redemption-shell" dir="rtl"><section className="glass-card rounded-3xl p-5 sm:p-7">{mode === "choose" ? <><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl gold-gradient text-white shadow-lg"><Building2 className="h-7 w-7" /></div><div className="mt-5 text-center"><h1 className="font-display text-2xl font-bold text-foreground">مرحباً بك في ترتيل</h1><p className="mt-2 text-sm leading-7 text-muted-foreground">اختر الطريقة المناسبة لبدء استخدام المنصة. لكل مركز بياناته وصلاحياته وإدارته المستقلة.</p></div><div className="mt-6 grid gap-3"><button onClick={() => setMode("create")} className="rounded-2xl border border-primary/25 bg-primary/5 p-4 text-right transition-colors hover:bg-primary/10"><span className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-primary text-primary-foreground"><Building2 className="h-5 w-5" /></span><span><strong className="block text-foreground">إنشاء مركز جديد</strong><span className="mt-1 block text-xs leading-5 text-muted-foreground">أنشئ مركزك المستقل وكن مديره الأول لإضافة الحلقات والمعلمين والطلاب.</span></span><ChevronLeft className="mr-auto mt-2 h-4 w-4 text-primary" /></span></button><button onClick={() => navigate("/redeem-access-code")} className="rounded-2xl border border-border bg-background/60 p-4 text-right transition-colors hover:bg-muted"><span className="flex items-start gap-3"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-muted text-primary"><KeyRound className="h-5 w-5" /></span><span><strong className="block text-foreground">الانضمام إلى مركز قائم</strong><span className="mt-1 block text-xs leading-5 text-muted-foreground">لديك كود دخول من مدير مركز أو معلم وتريد تفعيل صلاحياتك.</span></span><ChevronLeft className="mr-auto mt-2 h-4 w-4 text-muted-foreground" /></span></button></div></> : mode === "created" ? <><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-primary text-primary-foreground shadow-lg"><ShieldCheck className="h-7 w-7" /></div><div className="mt-5 text-center"><h1 className="font-display text-2xl font-bold text-foreground">تم إنشاء مركزك بنجاح</h1><p className="mt-2 text-sm leading-7 text-muted-foreground">أنت الآن مدير ومالك هذا المركز. ابدأ بإضافة الحلقة الأولى، ثم أضف المعلم والطلاب، أو انتقل إلى لوحة التحكم.</p></div><div className="mt-6 grid gap-3"><button onClick={() => navigate("/circles")} className="btn-gold w-full py-3">إعداد الحلقة الأولى</button><button onClick={() => navigate("/dashboard")} className="w-full rounded-xl border border-border bg-background px-4 py-3 text-sm font-semibold text-primary hover:bg-muted">فتح لوحة التحكم</button></div><p className="mt-4 text-center text-xs leading-6 text-muted-foreground">يمكنك إكمال هذه الخطوات لاحقاً؛ لن يظهر في التقارير أي نشاط قبل اعتماد أول جلسة.</p></> : <><button onClick={() => setMode("choose")} className="inline-flex items-center gap-1 text-sm font-semibold text-primary hover:underline"><ArrowRight className="h-4 w-4" />العودة للاختيار</button><div className="mt-4 flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-primary text-primary-foreground"><Building2 className="h-5 w-5" /></span><div><h1 className="font-display text-2xl font-bold text-foreground">إنشاء مركز مستقل</h1><p className="mt-1 text-sm text-muted-foreground">سيُربط حسابك تلقائياً كمالك ومدير لهذا المركز فقط.</p></div></div><form className="mt-6 space-y-4" onSubmit={submit}><label className="block"><span className="mb-1.5 block text-sm font-semibold">اسم المركز <span className="text-destructive">*</span></span><input value={centerName} onChange={(event) => setCenterName(event.target.value)} className="tarteel-input" placeholder="مثال: مركز النور لتحفيظ القرآن" maxLength={255} required /></label><label className="block"><span className="mb-1.5 block text-sm font-semibold">اسم الجهة التابعة <span className="text-muted-foreground">(اختياري)</span></span><input value={organizationName} onChange={(event) => setOrganizationName(event.target.value)} className="tarteel-input" placeholder="اتركه فارغاً لاستخدام اسم المركز" maxLength={255} /></label><div className="grid gap-3 sm:grid-cols-2"><label className="block"><span className="mb-1.5 flex items-center gap-1 text-sm font-semibold"><MapPin className="h-4 w-4 text-primary" />المدينة <span className="text-muted-foreground">(اختياري)</span></span><input value={city} onChange={(event) => setCity(event.target.value)} className="tarteel-input" maxLength={100} /></label><label className="block"><span className="mb-1.5 flex items-center gap-1 text-sm font-semibold"><Phone className="h-4 w-4 text-primary" />الهاتف <span className="text-muted-foreground">(اختياري)</span></span><input dir="ltr" value={phone} onChange={(event) => setPhone(event.target.value)} className="tarteel-input" maxLength={32} /></label></div><label className="block"><span className="mb-1.5 block text-sm font-semibold">نبذة <span className="text-muted-foreground">(اختياري)</span></span><textarea value={description} onChange={(event) => setDescription(event.target.value)} rows={3} className="tarteel-input resize-none" maxLength={5000} placeholder="نبذة موجزة عن المركز" /></label><div className="flex gap-3 rounded-2xl border border-primary/15 bg-primary/5 p-3 text-xs leading-6 text-muted-foreground"><ShieldCheck className="h-5 w-5 shrink-0 text-primary" /><p>سيُنشأ مركز ومنظمة مستقلة، وتُمنح لحسابك عضوية مالك للمركز ونطاق إدارة كامل. لا يمنحك ذلك أي وصول إلى مراكز أخرى.</p></div><button disabled={createCenter.isPending} className="btn-gold w-full py-3">{createCenter.isPending ? "جارٍ إنشاء مركزك…" : "إنشاء المركز وبدء الإدارة"}</button></form></>}</section></main>;
}
